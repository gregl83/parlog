{
  "logFileMatch": {
    "filename": ".*\\.log",
    "modifiers": ""
  },
  "logMatch": {
    "combined": {
      "format": "^([^ ]+) \\- ([^ ]+) \\[([^\\]]+)\\] \"([A-Z]+) ([^ ]+) HTTP\/([^ ^\"]+)\" ([^ ]+) ([^ ]+) \"([^\"]+)\" \"([^\"]+)\"$",
      "params": {
        "remote-addr": "string",
        "remote-user": "string",
        "date": "date",
        "method": "string",
        "url": "url",
        "http-version": "string",
        "status": "string",
        "content-length": "string",
        "referrer": "url",
        "user-agent": "string"
      }
    },
    "common": {
      "format": "^([^ ]+) \\- ([^ ]+) \\[([^\\]]+)\\] \"([A-Z]+) ([^ ]+) HTTP\/([^ ^\"]+)\" ([^ ]+) ([^ ]+)$",
      "params": {
        "remote-addr": "string",
        "remote-user": "string",
        "date": "date",
        "method": "string",
        "url": "url",
        "http-version": "string",
        "status": "string",
        "content-length": "string"
      }
    },
    "dev": "",
    "short": "",
    "tiny": ""
  }
}